% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ExecutionSettings.R
\name{resolveResultsPath}
\alias{resolveResultsPath}
\title{Resolve a task results path}
\usage{
resolveResultsPath(
  executionSettings,
  pipelineVersion,
  taskName = NULL,
  executionContext = NULL,
  execPath = here::here("exec/results")
)
}
\arguments{
\item{executionSettings}{An \code{ExecutionSettings} object; supplies the database name.}

\item{pipelineVersion}{Character. Test namespace (e.g. \code{"develop_ml"}) or
semantic version (e.g. \code{"1.0.2"}). Ignored when \code{executionContext} is supplied.}

\item{taskName}{Character or \code{NULL}. Task folder name appended to the path.}

\item{executionContext}{Optional \code{ExecutionContext} for the current run.}

\item{execPath}{Character. Base results path. Defaults to \code{exec/results} in
the current study project. Ignored when \code{executionContext} is supplied.}
}
\value{
Character. The absolute results path (not created).
}
\description{
Returns the absolute \verb{exec/results/<database>/<pipelineVersion>/<taskName>}
path for a run. When an \code{ExecutionContext} is supplied it is used directly;
otherwise an equivalent one is derived from the \code{ExecutionSettings} object
and the pipeline version. This keeps every results-folder path in the
package flowing through \link{ExecutionContext}'s \code{getResultsPath()} rather than
being hand-assembled at each call site.
}
\keyword{internal}
