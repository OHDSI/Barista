% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ExecutionSettings.R
\name{newExecutionContext}
\alias{newExecutionContext}
\title{Build an ExecutionContext from a pipeline version string}
\usage{
newExecutionContext(
  pipelineVersion,
  testMode = NULL,
  databaseName = NULL,
  execPath = here::here("exec/results")
)
}
\arguments{
\item{pipelineVersion}{Character. A semantic version, \code{"prod"}, or a test
namespace.}

\item{testMode}{Logical or \code{NULL}. \code{NULL} (default) infers the mode from
\code{pipelineVersion} via \code{\link[=isProductionPipelineVersion]{isProductionPipelineVersion()}}; \code{TRUE}/\code{FALSE} forces
it (used by the pipeline, which already knows).}

\item{databaseName}{Character or \code{NULL}. Passed through to \code{ExecutionContext}.}

\item{execPath}{Character. Base results path. Passed through.}
}
\value{
An \code{ExecutionContext}.
}
\description{
The single place that turns a raw \code{pipelineVersion} into an
\link{ExecutionContext}: it classifies the mode (unless \code{testMode} forces it) and
derives \code{studyVersion}, so callers never hand-roll \verb{mode = } /
\verb{studyVersion = }.
}
\keyword{internal}
