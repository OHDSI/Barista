# Format the full detail of a condition (message + call) for the log file. The console already gets the short version via cli; this is the durable copy.

Format the full detail of a condition (message + call) for the log file.
The console already gets the short version via cli; this is the durable
copy.

## Usage

``` r
formatErrorDetail(e)
```
